const moment = require("moment");
const axios = require("axios");
const assert = require("assert");
const { faker } = require("@faker-js/faker");

async function main() {
  // inject the script here that you want to run from the momentic app
}

main();
